# Jichu Li Academic Homepage

A dependency-free static academic homepage for GitHub Pages.

## Maintain the site

- Edit `index.html` to update text, links, layout, or styling.
- Replace files in `images/`, `logos/`, or `files/` when an asset changes.

